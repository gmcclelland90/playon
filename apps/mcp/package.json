{
  "name": "@playon/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "playon-mcp": "./dist/stdio-proxy.js"
  },
  "scripts": {
    "dev": "tsx src/stdio-proxy.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/stdio-proxy.js",
    "check": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.32",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
