/** Re-export shared allowlisted probe helpers for node-agent jobs. */
export {
  assertPackPathAllowed,
  expandScanRoots,
  matchHintsAt,
  runImportProbe,
} from "@playon/shared/import-probe-walk";
