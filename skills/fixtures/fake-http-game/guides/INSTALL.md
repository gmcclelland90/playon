# Fake HTTP Game

Hermetic fixture for autonomous tests.

1. Create a container (or mock container) named `fake-http-game`.
2. Expose TCP 8080.
3. Publish a player panel join block with `http://<host>:8080`.
