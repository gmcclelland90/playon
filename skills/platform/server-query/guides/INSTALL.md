# Server query platform skill

Use built-in `queryDialect` values from game skills when available. For novel games, follow [AUTHORING_CONNECTORS.md](AUTHORING_CONNECTORS.md).

Health checks may include `type: query_responding` so the monitor treats a successful live query as readiness.
