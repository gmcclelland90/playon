# Snapshot policy

- Create a **baseline** snapshot after first successful create.
- Snapshot before restores (automatic pre-restore).
- Prefer agent-assisted restore: “roll back before the last mod install”.
- Keep retention modest on LAN hosts (disk fills fast with world copies).
